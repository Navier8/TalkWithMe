# Copy this file to .env and fill in real values. .env is gitignored.

# Sent as "Authorization: Bearer <key>" to the LLM server configured in
# settings.yaml (llm.base_url). Optional — leave blank for a local
# llama.cpp server that doesn't require auth.
TALKWITHME_LLM_API_KEY=
